export const createGame = (gameName: string, gameCode: string, onePlayerBid: number, playersCount: number) => {
    console.log("Game Created:", { gameName, gameCode, onePlayerBid, playersCount });
    // Implement backend call or state update logic here
};